# Android Protocol Artifacts

This directory contains generated Android parity artifacts for the protocol contract.

These files are regenerated from the repository root when protocol contract output changes:

```bash
npm run protocol:contract
```

Treat generated sources here as reviewable outputs, not primary implementation files.
