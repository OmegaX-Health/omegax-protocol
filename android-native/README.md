# Android Native

This directory contains Android-facing parity artifacts derived from the protocol contract.

These files exist so mobile or native client implementations can stay aligned with the checked-in protocol surface without depending on a private build step.

See [protocol/README.md](./protocol/README.md) for the generated artifact note.
