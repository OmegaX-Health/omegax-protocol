# Shared Artifacts

This directory contains generated protocol artifacts that are consumed by client implementations.

Current artifact:

- `protocol_contract.json`

Regenerate from the repository root with:

```bash
npm run protocol:contract
```

Do not hand-edit generated artifacts unless a maintenance workflow explicitly requires it.
